---
import Layout from "~/layouts/PageLayout.astro";
import { SITE, getLanguageFromPath } from "~/config.mjs";

import Datatable from "@dbs/datatables";
import { Banner, i18n } from "@dbs/ui";

const countriesResponse = await fetch(`${SITE.apiUrl}/countries.json`);
const countries = await countriesResponse.json();
const locale = getLanguageFromPath(Astro.url);
---

<Layout>
  <Banner background="banner_countries" title="Countries" />
  <Datatable inputData={countries} locale={locale} tableType="countries" client:visible />
</Layout>
