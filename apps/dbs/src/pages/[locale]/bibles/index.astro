---
import Layout from "~/layouts/Wiki/BibleListLayout.astro";
import { SITE } from '~/config.js';

export const getStaticPaths = () => {
	return Object.keys(SITE.locales).map((locale) => ({
		params: { locale }
	})
)}
---

<Layout />